# Ralph Progress Log
Started: Mon Mar 23 22:28:44 CDT 2026
## Codebase Patterns
- Use `pnpm` as the package manager (not npm)
- Lint with `npx ultracite@latest check` — enforces Biome formatting (no trailing commas in single-line objects, sorted imports, numeric separators for large numbers)
- TypeScript check with `npx tsc --noEmit`
- Trailing commas in multi-line function params are NOT used by this formatter
- Existing auth uses NextAuth for regular users; admin auth is separate (env vars + JWT cookies)
- `jose` library used for JWT signing/verification in admin auth
---

## 2026-03-23 22:30 CDT - US-001
- Implemented POST /api/admin/auth endpoint and lib/admin/session.ts utility
- Files changed: lib/admin/session.ts (new), app/api/admin/auth/route.ts (new), package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - jose library was not installed — had to add it with pnpm
  - Biome linter requires: sorted imports (jwtVerify before SignJWT), numeric separators (86_400), no trailing commas in single-line args
  - `createSessionToken` returns a Promise from `.sign()` but doesn't need `async` keyword since it just returns the promise directly
  - `getAdminSessionFromRequest` parses cookies from raw header — useful for middleware contexts where `next/headers` cookies() isn't available
---

## 2026-03-23 - US-002
- Verified admin auth verification utility already fully implemented in US-001
- `verifyAdminSession()` reads admin-session cookie from next/headers, returns `{ authenticated: boolean }`
- `getAdminSessionFromRequest(request)` parses cookies from raw header for API route/middleware use
- Handles missing cookie, expired token, invalid signature gracefully (try/catch returns `{ authenticated: false }`)
- Files changed: prd.json (marked passes: true)
- **Learnings for future iterations:**
  - US-001 already implemented all of US-002's requirements — sometimes stories overlap
  - Pre-existing lint errors (806) exist across the codebase; don't be alarmed, just ensure your own files are clean
  - `npx tsc --noEmit` passes cleanly
---

## 2026-03-23 - US-003
- Wired up admin login form to POST /api/admin/auth
- Added loading state (LOADING... text, disabled button), error display, and auto-clear on typing
- On success redirects to /admin/dashboard, on 401 shows INVALID CREDENTIALS, on network error shows CONNECTION ERROR
- Files changed: app/admin/page.tsx, prd.json
- **Learnings for future iterations:**
  - The login page uses `useRouter` from `next/navigation` for client-side redirect
  - Error messages appear below the submit button in red, consistent with the retro theme
  - The `Link` import from `next/link` was already present but unused for this feature (used for BACK TO SITE link)
---

## 2026-03-23 - US-004
- Created middleware.ts at project root for admin route protection
- Protects /admin/* routes (except /admin login page and /api/admin/auth)
- Uses jose jwtVerify directly (edge-compatible, can't import lib/admin/session.ts in middleware edge runtime)
- Redirects unauthenticated users to /admin
- Files changed: middleware.ts (new), prd.json
- **Learnings for future iterations:**
  - Next.js middleware runs in Edge runtime — can't import Node.js-only modules
  - jose works in Edge runtime natively
  - Used NextRequest.cookies.get() API instead of parsing raw cookie header
  - Matcher `/admin/:path+` matches all sub-paths but NOT `/admin` itself
---

## 2026-03-23 - US-005
- Created POST /api/admin/auth/logout route
- Clears admin-session cookie by setting maxAge: 0
- Returns { success: true }
- Files changed: app/api/admin/auth/logout/route.ts (new), prd.json
- **Learnings for future iterations:**
  - Cookie clearing uses same path ("/admin") as the original cookie set
  - The logout route is excluded from middleware protection since it's under /api/admin/auth
---

## 2026-03-23 - US-006
- Created app/admin/(portal)/layout.tsx with sidebar navigation
- Nav links: Dashboard, Companies, Subscribers, Scrape Status, Settings
- Active route highlighted with gold left border and text
- LOGOUT button at bottom calls POST /api/admin/auth/logout then redirects to /admin
- Mobile hamburger menu with overlay backdrop
- Files changed: app/admin/(portal)/layout.tsx (new), prd.json
- **Learnings for future iterations:**
  - Route group `(portal)` wraps authenticated admin pages without affecting URL
  - `usePathname().startsWith(href)` for active nav highlighting
  - The parent admin layout.tsx provides the Press Start 2P font variable
  - Mobile sidebar uses translate-x transform with md: breakpoint
---

## 2026-03-23 - US-007
- Created GET /api/admin/companies proxy route to kbz_chron /search
- Forwards query params (q, company_name, sector, series, investor, days, limit)
- 10-second AbortController timeout, admin session verification, 502 on upstream errors
- Files changed: app/api/admin/companies/route.ts (new), prd.json
- **Learnings for future iterations:**
  - Use AbortController with setTimeout for fetch timeouts (no native timeout option)
  - KBZ_CHRON_URL defaults to https://kbz-chron.fly.dev
  - Use `verifyAdminSession()` from lib/admin/session.ts for cookie-based auth in API routes
---

## 2026-03-23 - US-008
- Created GET /api/admin/scrape/health proxying to kbz_chron /health
- Created GET /api/admin/scrape/status proxying to kbz_chron / (root)
- Both verify admin session, handle upstream errors with 502, 10s timeout
- Files changed: app/api/admin/scrape/health/route.ts (new), app/api/admin/scrape/status/route.ts (new), prd.json
- **Learnings for future iterations:**
  - Proxy routes follow same pattern: verify session, AbortController timeout, try/catch with 502
---

## 2026-03-23 - US-009
- Created /admin/dashboard page with metric cards and recent companies
- Metric cards: Total Subscribers, Scrape Health (green/red), Last Scrape Time
- Recent Companies preview list (last 5 from past 7 days) with links to detail
- Loading skeletons (animate-pulse), error states for each section
- Files changed: app/admin/(portal)/dashboard/page.tsx (new), prd.json
- **Learnings for future iterations:**
  - /api/subscribe GET returns array of subscribers (use .length for count)
  - kbz_chron health returns { status: "healthy" } or similar
  - kbz_chron root returns { last_run: { timestamp: ... } } for last scrape info
  - Companies API may return array directly or { results: [...] } — handle both
---

## 2026-03-23 - US-010
- Created /admin/companies page with search, sector/series filters, pagination
- Table: Company Name, Funding Amount, Series, Sector, Source, Date
- Client-side pagination (20 per page), search triggers API re-fetch
- Company names link to /admin/companies/[name] detail page
- Loading skeleton, error state, empty state
- Files changed: app/admin/(portal)/companies/page.tsx (new), prd.json
- **Learnings for future iterations:**
  - Search uses q param on API, filters use sector/series params
  - Default 30 days of data, limit 200 for client-side pagination
  - useCallback for fetchCompanies to avoid stale closure in useEffect
---

## 2026-03-23 - US-011
- Created /admin/companies/[name] detail page
- Displays all company fields in card layout
- URL field renders as clickable link, investors array joined with commas
- Back button to companies list, loading/error/not-found states
- Files changed: app/admin/(portal)/companies/[name]/page.tsx (new), prd.json
- **Learnings for future iterations:**
  - Next.js 15 App Router: params is a Promise, use React `use()` to unwrap
  - Company name from URL is already encoded, need decodeURIComponent for display (but API handles it)
---

## 2026-03-23 - US-012
- Created /admin/scrape page with health indicator, last run details, trigger button (disabled)
- Auto-refreshes every 30 seconds
- Shows health green/red, last run timestamp/status/articles/companies counts
- Trigger Scrape button disabled with "Coming soon" tooltip
- Files changed: app/admin/(portal)/scrape/page.tsx (new), prd.json
---

## 2026-03-23 - US-013
- Created POST /api/admin/scrape/trigger proxy to kbz_chron /run
- 120-second timeout for long scraping operations
- Admin session verification, 502 on upstream errors
- Files changed: app/api/admin/scrape/trigger/route.ts (new), prd.json
---

## 2026-03-23 - US-014
- Wired trigger scrape button on scrape status page
- Confirmation dialog before triggering, SCRAPING... loading state
- Success shows result summary, error shows red message
- Refreshes status data after successful scrape
- Files changed: app/admin/(portal)/scrape/page.tsx, prd.json
---

## 2026-03-23 - US-015
- Created /admin/subscribers page with search
- Table: Email, Agreed to TOS, Signed Up date
- Client-side email filter, total count, sorted by createdAt desc (from API)
- Loading skeleton, error state, empty states
- Files changed: app/admin/(portal)/subscribers/page.tsx (new), prd.json
- **Learnings for future iterations:**
  - /api/subscribe GET returns { subscribers: [...] } not a raw array
  - Subscribers already sorted by createdAt desc from the query
---
